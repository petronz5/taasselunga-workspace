apiVersion: v1
kind: Service
metadata:
  name: taasselunga-frontend
spec:
  type: NodePort
  selector:
    app: taasselunga-frontend
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30000